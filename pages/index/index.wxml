<mp-navigation-bar color="{{color}}" background="{{background}}" title="热点" back="{{false}}"></mp-navigation-bar>
<view class="container">
  <view class="item" wx:for="{{list}}" wx:for-index="idx" wx:key="idx">
    【{{item.id === 0? '置顶' : item.id}}】{{item.title}}
  </view>
</view>